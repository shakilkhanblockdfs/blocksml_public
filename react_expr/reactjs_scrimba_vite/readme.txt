npm create vite@latest     # Initially once only for new project
cd vite-project
npm install           # Install each time the new code is checkout from github as node_mules are not installed
npm run dev