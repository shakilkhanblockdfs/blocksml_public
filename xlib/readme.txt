recursively list out all files and folders in a directory
Check if a process exist for Linux as well as Windows.
Make a comment or note as to which API modifies the original argumewnt and which returns a new copy
File read/write and buffering
basename basedir, basedrive from complete path
